import { registry } from "../nodes/registry.js";
/** @import { Node } from "../nodes/registry.js" */

/**
 * Recursive paint walk.
 * @param {Node} node - The root of the tree to paint
 * @param {*} canvas - The canvas to paint into
 * @todo Implement
 */
export function paint(node, canvas) {
	throw new Error("Not implemented");
}
