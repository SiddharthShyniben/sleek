import { registry } from "../nodes/registry.js";
/** @import { Node } from "../nodes/registry.js" */

/**
 * Recursive layout algorithm.
 * @param {Node} node - The root of the tree to lay out
 * @param {*} constraints - Available space to lay out within
 * @todo Implement
 */
export function layout(node, constraints) {
	throw new Error("Not implemented");
}
